package hellojpa;

public class ManyToMany {
    /*
        다대다
        관계형 데이터베이스는 정규화된 테이블 2개로 다대다 관계를 표현할 수 없음.
        연결 테이블으 추가해서 일대다, 다대일 관계로 풀어내야함.

        연결 테이블을 추가해서 일대다, 다대일 관계로 풀어내야함.

        객체는 컬렉션을 사용해서 객체 2개로 다대다 관계 가능

        @ManyToMany 사용
        @JoinTable 로 연결 테이블 지정
        다대다 매핑 : 단방향, 양방향 가능

        다대다 매핑의 한계

     */
}
